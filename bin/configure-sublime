#!/bin/bash
# check if we've got the ~/.config/sublime directory; prompt to run create-symlinks if not
# mv ~/Library/Application Support/Sublime Text 3/Packages/User /tmp
# ln -s ~/.config/sublime/Packages/User ~/Library/Application Support/Sublime Text 3/Packages/User
# copy /tmp/User over the top of ~/.config/sublime/Packages/User
# prompt to do a git diff
# you still need to install packages